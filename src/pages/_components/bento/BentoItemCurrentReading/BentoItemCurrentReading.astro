---
import client from '@/lib/client'

import BentoItemCurrentReadingReact from './BentoItemCurrentReading'

const initialData = await client.api.reading.$get().then((res) => res.json())
---

<BentoItemCurrentReadingReact initialData={initialData} client:only='react' />
